<%= form_tag search_path, :method => 'get' do %>
  <div class="large-6 small-3 columns search text-left">
    <%= text_field_tag :search, params[:search], placeholder: ("<i class='fi-magnifying-glass'></i>").html_safe %>
    <%= submit_tag 'Search', :style => "display:none" %>
  </div>
<% end %>