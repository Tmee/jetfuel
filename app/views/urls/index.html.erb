 <div class="row">
  <h1>Shortener..er</h1>
    <%=form_for Url.new do |f|%>
      <%= f.url_field :full_url, placeholder: "Please include http://", id: 'url_new' %>
      <%= f.submit "begin Shortener", class: 'button tiny round'%>
    <%end%><br>

  <% @urls.each do |url|%>
    <div class="large-6 columns panel">
      <ul>
        <li><%= url.full_url %></li>
        <li><a href="#" data-reveal-id="myModal"><%= link_to "shorterish link", url_path(url.id)%></a></li>
        <li><%= url.popularity %></li>
        <li><%= url.title %></li>
      </ul>
    </div>
  <% end %>
</div>